name: Hot News Crawler

on:
  schedule:
    # 每天早中晚三次：08:00、12:00、18:00（北京时间）
    - cron: "0 0,4,10 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest

    steps:
      # -------------------------------
      # 1. 拉取仓库
      # -------------------------------
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # -------------------------------
      # 2. 与远端 master 同步（避免冲突）
      # -------------------------------
      - name: Sync with remote master
        run: |
          git fetch origin master
          git reset --hard origin/master

      # -------------------------------
      # 3. 设置 Python
      # -------------------------------
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # -------------------------------
      # 4. 检查配置文件
      # -------------------------------
      - name: Verify required files
        run: |
          echo "🔍 检查必要配置文件..."

          if [ ! -f config/config.yaml ]; then
            echo "❌ config/config.yaml 不存在"
            exit 1
          fi

          if [ ! -f config/frequency_words.txt ]; then
            echo "❌ config/frequency_words.txt 不存在"
            exit 1
          fi

          echo "✅ 配置检查通过"

      # -------------------------------
      # 5. 运行爬虫
      # -------------------------------
      - name: Run crawler
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DINGTALK_WEBHOOK_URL: ${{ secrets.DINGTALK_WEBHOOK_URL }}
          WEWORK_WEBHOOK_URL: ${{ secrets.WEWORK_WEBHOOK_URL }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_SERVER_URL: ${{ secrets.NTFY_SERVER_URL }}
          NTFY_TOKEN: ${{ secrets.NTFY_TOKEN }}
          GITHUB_ACTIONS: true
        run: python main.py

      # -------------------------------
      # 6. 提交并推送结果
      # -------------------------------
      - name: Commit and push if changes
        run: |
          set -e

          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

          git add -A

          if git diff --quiet && git diff --staged --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Auto update by GitHub Actions at $(TZ=Asia/Shanghai date)"

          # 防止冲突，先 pull --rebase
          git pull origin master --rebase --allow-unrelated-histories || true

          git push origin master
